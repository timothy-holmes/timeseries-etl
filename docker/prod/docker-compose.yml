version: "3"

services:
    timeseries-etl:
        container_name: ts-etl-prod
        build:
            context: ../../
            dockerfile: ./docker/prod/Dockerfile
        volumes:
            - ../../data:/app/data
        command: bash -c "python /app/src/run.py"

